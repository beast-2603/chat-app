{
  "rules": {
    "profiles": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    },
    "rooms": {
      ".read": "auth !== null",
      ".write": "auth !== null",
      "$user_id": {
        ".read": "auth !== null",
        ".write": "auth !== null"
      }
    },
    "messages": {
      "$message_id": {
        ".read": "auth !== null",
        ".write": "auth !== null"
      }
    },
    "status": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    },
    ".read": false,
    ".write": false
  }
}
